version: '3.8'

services:
  santa:
    build: .
    container_name: santa-app
    ports:
      - "8080:8080"
    volumes:
      - ./data:/tmp
    environment:
      - DATABASE_URL=sqlite:////tmp/santa.db
